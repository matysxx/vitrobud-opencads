# OCSSTACK-15 Close Public Container Documentation Gaps

## Requirements

- [ ] State plainly in the root README that the maintained fork targets a
  private Debian runtime with rootless Podman and `systemd --user` autostart.
- [ ] State that Debian builds the OCI image from the exact verified GitHub
  revision and that TLS/reverse proxy remain external.
- [ ] Add one public documentation index covering every fork-specific runbook.
- [ ] Separate fork/container documentation from inherited upstream application
  documentation without deleting upstream attribution or guides.
- [ ] Update the R12 exporter guide for the current direct `cadcodec` baseline.
- [ ] Keep unfinished local OCSSTACK-6 and OCSSTACK-12 material out of GitHub.
- [ ] Verify links, privacy boundaries, and required `.ai/wiki` behavior.
