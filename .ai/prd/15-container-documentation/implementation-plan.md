# OCSSTACK-15 Implementation Plan

1. Expand the root README maintained-fork section with the Debian, Podman,
   systemd-user, immutable-revision, and external-TLS runtime contract.
2. Add `docs/README.md` as the canonical index of fork/container operations,
   maintenance, security, migration, backup, and R12 compatibility guides.
3. Replace obsolete acadifc-specific rationale in the R12 guide with the
   current direct cadcodec and isolated compatibility-writer boundary.
4. Run local-link, privacy, required-file, ignore-policy, and diff checks.
5. Commit with Conventional Commits and publish through local `main` to the
   anonymized GitHub `main`; do not operate on the server without SSH approval.
